{
  "name": "solana-tx-sre-verify",
  "private": true,
  "version": "0.0.0",
  "description": "Type-check harness for solana-tx-sre. Extracts the TS blocks straight out of skill/*.md and compiles them, so the shipped prose itself is the tested artifact. CI-only; not part of the installed skill.",
  "license": "MIT",
  "scripts": {
    "extract": "node scripts/extract-snippets.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check": "npm run extract && npm run typecheck"
  },
  "devDependencies": {
    "@solana/web3.js": "^1.95.3",
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2"
  }
}
